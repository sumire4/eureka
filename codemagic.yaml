workflows:
  ios-workflow:
    name: iOS Debug Build (No Code Signing)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: 15.0
      cocoapods: default
      groups:
        # OpenAI API key'inizi buraya ekleyin (Codemagic environment variables'dan)
        - app_secrets
      vars:
        # Bundle ID'nizi değiştirin
        BUNDLE_ID: "com.yourname.eureka"
        XCODE_WORKSPACE: "Runner.xcworkspace"
        XCODE_SCHEME: "Runner"
    
    scripts:
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      
      - name: Get Flutter packages
        script: |
          flutter pub get
      
      - name: Flutter analyze
        script: |
          flutter analyze
      
      - name: Build iOS (Debug - No Code Signing)
        script: |
          flutter build ios --debug --no-codesign
      
      - name: Create IPA (manual signing)
        script: |
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -r app-debug.ipa Payload
          mv app-debug.ipa $CM_BUILD_DIR/
    
    artifacts:
      - app-debug.ipa
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true

  # Release build için Apple Developer hesabınız varsa bu workflow'u kullanın
  # Önce Codemagic'te App Store Connect entegrasyonunu yapılandırmalısınız
  ios-release-workflow:
    name: iOS Release Build (Requires Apple Developer Account)
    max_build_duration: 60
    instance_type: mac_mini_m2
    
    # App Store Connect entegrasyonu
    integrations:
      app_store_connect: codemagic  # Codemagic'te ayarladığınız entegrasyon adı
    
    environment:
      flutter: stable
      xcode: 15.0
      cocoapods: default
      groups:
        - app_secrets
      vars:
        BUNDLE_ID: "com.yourname.eureka"
        XCODE_WORKSPACE: "Runner.xcworkspace"
        XCODE_SCHEME: "Runner"
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.yourname.eureka
    
    scripts:
      - name: Set up keychain
        script: |
          keychain initialize
      
      - name: Fetch signing files
        script: |
          app-store-connect fetch-signing-files $BUNDLE_ID \
            --type IOS_APP_STORE \
            --create
      
      - name: Use certificate and profile
        script: |
          keychain add-certificates
          xcode-project use-profiles
      
      - name: Get Flutter packages
        script: |
          flutter pub get
      
      - name: Build iOS Release
        script: |
          flutter build ipa --release \
            --export-options-plist=$HOME/export_options.plist
    
    artifacts:
      - build/ios/ipa/*.ipa
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    
    publishing:
      app_store_connect:
        # Codemagic'te yapılandırdığınız App Store Connect API entegrasyonunu kullanır
        submit_to_testflight: true
        # submit_to_app_store: true  # App Store'a direkt göndermek için
      
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true
